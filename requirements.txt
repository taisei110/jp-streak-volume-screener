# 公開アプリ(Streamlit)の実行に必要な依存。
# `pip install -r requirements.txt` で導入できる。
streamlit>=1.52
duckdb>=1.1
pandas>=2.2
yfinance>=0.2.66
jpholiday>=0.1.0
# 銘柄ユニバースをJPXのExcel(.xls)から取得する場合に必要(同梱サンプルCSV利用時は不要)
openpyxl>=3.1
xlrd>=2.0
